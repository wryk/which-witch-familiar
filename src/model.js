export type Instance = string

export type Username = string

export type Account = {
	instance: Instance,
	username: Username
}

export type Familiar = {
	name: string,
	race: string,
	emoji: string,
	description: string
}
